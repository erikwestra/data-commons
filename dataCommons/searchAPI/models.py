""" dataCommons.searchAPI.models

    This file contains the Django models used by the Search API.
"""
from django.db import models

#############################################################################

# This module is deliberately empty, because the Search API doesn't define any
# database models.
