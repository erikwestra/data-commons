""" dataCommons.admin.models

    This file contains the Django models used by the "admin" application.
"""
from django.db import models

#############################################################################

# This module is deliberately empty, because the admin application doesn't
# define any database models.
